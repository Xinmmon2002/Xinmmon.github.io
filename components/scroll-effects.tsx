'use client';
import { useEffect,useRef } from 'react';
export function ScrollEffects(){const progress=useRef<HTMLDivElement>(null);useEffect(()=>{let frame=0;const update=()=>{const range=document.documentElement.scrollHeight-innerHeight;if(progress.current)progress.current.style.transform='scaleX('+(range>0?Math.min(1,Math.max(0,scrollY/range)):0)+')';frame=0};const onScroll=()=>{if(!frame)frame=requestAnimationFrame(update)};addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',onScroll);update();let observer:IntersectionObserver|undefined;if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&'IntersectionObserver' in window){document.documentElement.classList.add('motion-ready');observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer?.unobserve(e.target)}})},{rootMargin:'0px 0px 60px 0px',threshold:.03});document.querySelectorAll('.reveal').forEach(n=>observer!.observe(n))}return()=>{document.documentElement.classList.remove('motion-ready');observer?.disconnect();removeEventListener('scroll',onScroll);removeEventListener('resize',onScroll);if(frame)cancelAnimationFrame(frame)}},[]);return <div className="read-progress" ref={progress} aria-hidden="true"/>}
